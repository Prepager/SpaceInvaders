// Header
#include "Shield.h"

// Initialize the shield.
void initializeShield(Shield *shield) {
	//
}

// Check collision between bullet and shields.
Bullet* collidesShield(Shield *shield, Bullet *bullet) {
	//
	return bullet;
}
